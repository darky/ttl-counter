name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/setup-node@v1.1.0
      with:
        version: 10, 12, 13
    - name: Tests
      run: npm install && npm run test
